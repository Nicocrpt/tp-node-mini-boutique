# Projet mini-boutique

## Setup

Avant de démarrer le projet, il faut éxecuter la commande suivante pour télécharger les dépendances du projet :

```bash
npm install
```

Ensuite, il suffit de lancer le projet avec la commande :

```bash
npm run dev
```